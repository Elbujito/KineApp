import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AboutHomeComponent } from './about.component';

describe('AboutComponent', () => {

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AboutHomeComponent],
    })
      .compileComponents();
  }));

  beforeEach(() => {
  });
});
